apiVersion: v1
kind: Pod
spec:
    containers:
      - name: jnlp
        image: 'jenkinsci/jnlp-slave:alpine'
      - name: helm
        image: 'appdynamics/dotnet-core-agent'
        imagePullPolicy: IfNotPresent
        command:
          - /bin/cat
        tty: true